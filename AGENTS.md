# RunTrip 开发约束

- RunTrip 是 Android 16、个人单机、本地存储应用；未经明确确认，不加入账号、后端、联网、云同步、通知或多端能力。
- 首版字段、参赛状态和交互以当前产品约定为准；保持改动小而可追溯，不擅自扩展需求。
- 数据库存储稳定的英文 code；中文只作为 UI 展示文案，不直接持久化为状态或类型值。
- UI 使用中文，品牌名固定为 `RunTrip`；遵循 Material 3 的号码布风格、荧光黄绿色单一强调色，并同时维护浅色与深色模式。
- 数据结构需要可迁移；行为变化必须同步测试和文档。
